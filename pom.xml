<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <!-- WARNING: DO NOT MODIFY THIS FILE! This file was generated by mvngen #af50f1250c -->
  <modelVersion>4.0.0</modelVersion>

  <groupId>biz.descom.hellojava</groupId>
  <artifactId>root</artifactId>
  <version>1.0-SNAPSHOT</version>
  <name>Webapp Hello Java</name>
  <description>Demo Webapp for Containers and Clusters (docker / k8s / rancher etc.)</description>
  <packaging>pom</packaging>
  <organization>
    <name>Descom Consulting Ltd.</name>
  </organization>
  <scm>
    <connection>scm:git:https://remigius_stalder@bitbucket.org/remigius_stalder/webapp-hello-java.git</connection>
  </scm>

  <properties>
    <junit.version>4.12</junit.version>
    <postgresql.jdbc.version>42.1.4</postgresql.jdbc.version>
    <servlet.api.version>3.1.0</servlet.api.version>
    <surefire.plugin.version>2.21.0</surefire.plugin.version>
    <jar.plugin.version>3.1.0</jar.plugin.version>
    <buildnumber.plugin.version>1.4</buildnumber.plugin.version>
    <war.plugin.version>3.2.0</war.plugin.version>
    <compiler.plugin.version>3.7.0</compiler.plugin.version>
    <dockerfile.plugin.version>1.4.3</dockerfile.plugin.version>
    <resources.plugin.version>3.0.2</resources.plugin.version>
    <maven.build.timestamp.format>yyyy-MM-dd'T'HH:mm:ssXXX</maven.build.timestamp.format>
    <build.time>${maven.build.timestamp}</build.time>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <addMavenDescriptor>false</addMavenDescriptor>
    <projectRoot>.</projectRoot>
    <scmBranch/>
    <build.repo.branch>${scmBranch}</build.repo.branch>
    <build.repo.commit>${buildNumber}</build.repo.commit>
    <build.repo.url>https://remigius_stalder@bitbucket.org/remigius_stalder/webapp-hello-java.git</build.repo.url>
  </properties>

  <modules>
    <module>webapp</module>
  </modules>

  <build>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>buildnumber-maven-plugin</artifactId>
        <version>${buildnumber.plugin.version}</version>
        <executions>
          <execution>
            <id>createBuildNumber</id>
            <phase>validate</phase>
            <goals>
              <goal>create</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <shortRevisionLength>10</shortRevisionLength>
          <format>{0}</format>
          <items>
            <item>scmVersion</item>
          </items>
        </configuration>
      </plugin>
    </plugins>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${compiler.plugin.version}</version>
          <configuration>
            <source>1.8</source>
            <target>1.8</target>
            <testSource>1.8</testSource>
            <testTarget>1.8</testTarget>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${surefire.plugin.version}</version>
          <configuration>
            <argLine>-Dfile.encoding=${project.build.sourceEncoding}</argLine>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
  <profiles>
    <profile>
      <id>shell-unix</id>
      <activation>
        <os>
          <family>unix</family>
        </os>
      </activation>
      <properties>
        <shell.cmd>sh</shell.cmd>
        <shell.arg/>
      </properties>
    </profile>
    <profile>
      <id>shell-windows</id>
      <activation>
        <os>
          <family>windows</family>
        </os>
      </activation>
      <properties>
        <shell.cmd>cmd</shell.cmd>
        <shell.arg>/c</shell.arg>
      </properties>
    </profile>
  </profiles>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>junit</groupId>
        <artifactId>junit</artifactId>
        <version>${junit.version}</version>
        <scope>test</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>
</project>
